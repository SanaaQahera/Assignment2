    #include <iostream>
    #include <cmath>
    using namespace std;
     
    int main() {
    	double num,rt;
    	cout <<"Enter a number\n";
    	cin >>num;
    	rt = sqrt(num);
    	cout <<"The square root of number is =" <<rt;
    	return 0;
    }
